services:
  - type: web
    name: ith-flyer-generator
    runtime: node
    region: oregon
    plan: free
    rootDir: generator
    buildCommand: npm install && npx puppeteer browsers install chrome
    startCommand: node server.js
    envVars:
      - key: NODE_ENV
        value: production
      - key: PUPPETEER_CACHE_DIR
        value: /opt/render/.cache/puppeteer
